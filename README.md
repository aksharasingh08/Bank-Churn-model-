# Customer churn prediction model for bank

Uisng ANN on a bank dataset to predict wheater the person is likely to churn or not.
Used tensorflow library with Keras to train a sequential model with 2 dense hidden layers and a output layer.

Activation functions used:
* Sigmoid for output layer
* ReLU for hidden layers

Predicted the likelihood on a new input data.
